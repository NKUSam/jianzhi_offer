总结：
    两个栈实现队列，重点在于压栈和弹栈上。设：两个栈分别是栈A、栈B，两个栈实现的队列叫伪队列。
    压栈：如果栈A中有元素的话，判断栈B中有没有元素。如果有，说明伪队列中有大于等于2数量的元素。如果没有，说明伪队列中只有两个元素。
    如果栈A中没有元素，则压栈到栈A，如果有元素，则判断栈B中有没有元素。如果栈B没有元素，则直接对其进行压栈操作。如果有元素，则将
    其所有元素移到栈A中，并对栈B进行压栈。随后将栈A中元素移回到栈B并且将随后的栈B栈顶元素压栈到栈A。
